apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-package.fullname" . }}-test-connection"
  labels:
    {{- include "helm-package.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-package.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
